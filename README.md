This needs to work in conjunction at least with the PSF Header from the package at: https://gallery.nintex.com/t/k2_presales_framework

On a form the header view should be called "header" and you need to call its Server Side Rule to initialise.

The profile is designed to work with tabs which it relocates to the side bar it injects.

